---
description: 代码注释、TODO.md 与 DEVLOG.md 维护要求
alwaysApply: true
---

# 注释与开发日志约定

## 文件头注释(强制)
- 每个 .cs 文件开头必须有注释块,说明三点:该文件的作用、内部主要类型/结构、与其他模块的依赖关系。示例:

```csharp
// ============================================
// 文件: WorldTruthSystem.cs
// 作用: 主线剧情进度的单一事实来源(ModSystem),负责存档与读档
// 结构: StoryPhase 枚举 / WorldTruthSystem 类(进度字段 + 读写接口)
// 依赖: 被 NPCs/、UI/ 各模块读写;不依赖其他模块
// ============================================
```

- 正文中非显而易见的逻辑(设计意图、剧情关联、数值理由)必须注释;禁止写复述代码的废话注释。

## TODO.md(每模块一份)
- 模块目录 `World/`、`NPCs/TownNPCs/`、`NPCs/Bosses/`、`content/Items/`、`UI/` 各有一份 TODO.md。
- 开始任务前先读自己模块的 TODO.md;完成一项立即勾选;开发中发现的新工作追加到列表,不删除旧条目。

## DEVLOG.md(每模块一份 + 根目录总线一份)
- 每次新建、修改、删除、调整文件后,必须在**所在模块**的 DEVLOG.md 末尾追加记录。
- **task 分支上禁止修改根目录总线 DEVLOG.md**(它是主干和各分支的公共冲突热点)。任务完成的总结、跨模块调整、基础设施变更,统一在**合并进 master 之后**由合并者补记到总线 DEVLOG.md。
- 分支上产生的、值得总线记录的信息(如构建环境修复),先写进自己模块的 DEVLOG,合并时再摘录到总线。
- 记录格式(只追加,不改写历史):

```markdown
## 2026-07-04 | 分支或执行者
- [新建] World/WorldTruthSystem.cs:主线进度 ModSystem 骨架
- [修改] NPCs/TownNPCs/Etya.cs:对话池按剧情阶段切换
- [删除] xxx.cs:原因
```
